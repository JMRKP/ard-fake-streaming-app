/** Initial countdown duration before the stream starts (seconds) */
export const INITIAL_COUNTDOWN = 10;

/** Maximum stream duration (seconds) — 3 minutes */
export const STREAM_DURATION_SECONDS = 180;

/** Minimum Peak-O-Meter percentage required for a win */
export const WIN_THRESHOLD = 60;

/** Maximum value of the random component added to view count each second */
export const VIEW_COUNT_RANDOM_MAX = 500;

/** Minimum view count increment added each second */
export const VIEW_COUNT_RANDOM_MIN = 100;

/** Maximum Peak-O-Meter increase per second */
export const SUCCESS_LEVEL_INCREMENT_MAX = 2;

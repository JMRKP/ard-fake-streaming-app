{
  "name": "3-minutes-fake-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:pwa": "pnpm --filter pwa dev",
    "build:pwa": "pnpm --filter pwa build",
    "deploy": "pnpm --filter pwa deploy",
    "dev:controller": "pnpm --filter controller dev",
    "build:controller": "pnpm --filter controller build"
  },
  "pnpm": {
    "overrides": {
      "vite": "6.3.5"
    }
  }
}
